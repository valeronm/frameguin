# Pinned rather than left to `cargo fmt`, which infers it from each crate's
# edition: the style guide is versioned separately from the language, so a
# crate moving edition would silently reformat the tree.
style_edition = "2024"
