[package]
name = "frameguin-model"
version.workspace = true
edition = "2024"
license = "MIT"

[lints]
workspace = true

# Neither GTK nor a bus connection belongs here: the tray draws from this
# crate on ksni's own thread, a device is exercised in tests against a stub
# of its hardware trait, and the daemon links it into the root process for
# the words it logs a part in.
[dependencies]
frameguin-contract = { workspace = true }
