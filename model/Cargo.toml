[package]
name = "frameguin-model"
version.workspace = true
edition = "2024"
license = "MIT"

[lints]
workspace = true

# Neither GTK nor a bus connection belongs here: the tray draws from this
# crate on ksni's own thread, and a control is exercised in tests against a
# stub of its trait.
[features]
# The stub figures modelview's tests share.
testing = []

[dependencies]
frameguin-contract = { workspace = true }
