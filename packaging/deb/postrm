#!/bin/sh
set -e

# The daemon's StateDirectory mirrors the settings the hardware cannot report
# back. It is only a record of what was set, so purge may drop it; remove
# keeps it so reinstalling still reports the values the hardware is holding.
if [ "$1" = "purge" ]; then
    rm -rf /var/lib/frameguin
fi

#DEBHELPER#
