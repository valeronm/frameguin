[package]
name = "frameguin-wire"
version.workspace = true
edition = "2024"
license = "MIT"

[lints]
workspace = true

# Nothing that touches hardware belongs here: the app links this crate, and
# the daemon holding the only hardware access is the security model.
[dependencies]
frameguin-contract = { workspace = true }
zbus = { workspace = true }
