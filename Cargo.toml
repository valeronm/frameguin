[workspace]
members = ["daemon", "app", "wire"]
resolver = "2"

[workspace.package]
version = "0.1.2"

[workspace.dependencies]
frameguin-wire = { path = "wire" }
zbus = "5.19.0"

[workspace.lints.clippy]
pedantic = { level = "warn", priority = -1 }

[profile.release]
lto = true
strip = true
